/**
 * Test Fixtures
 * Basic test setup for E2E tests
 */

import { test as base, expect } from '@playwright/test';

// Basic test fixture
export const test = base.extend({
  // Add any custom fixtures here if needed
});

export { expect };